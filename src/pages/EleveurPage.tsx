/**
 * Éleveur (breeder) calculator — page shell.
 *
 * Routing is in place (Plan A); the calculator itself is Plan C. This shell
 * exists so the /eleveur route and navigation work today.
 */
export function EleveurPage() {
  return (
    <main>
      <section className="panel">
        <h2>Calculateur d'éleveur</h2>
        <p className="hint">
          Bientôt : de quoi évaluer l'élevage de dragodindes (reproduction,
          rentabilité…). Cette page est en place, le calcul arrive.
        </p>
      </section>
    </main>
  );
}
